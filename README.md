# Dzign Widgets

React component library for Dzign Studio. The package is built with Vite,
TypeScript, Tailwind CSS, Storybook, and shadcn/ui-style primitives, then
published to GitHub Packages as `@dzign-studio/dzign-widgets`.
